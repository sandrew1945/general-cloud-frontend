export const SET_TOKEN = (state, token) => {
  state.token = token
}

export const SET_NAME = (state, name) => {
  state.name = name
}

export const SET_AVATAR = (state, avatar) => {
  state.avatar = avatar
}

export const SET_ID = (state, id) => {
  state.id = id
}

export const SET_CODE = (state, code) => {
  state.code = code
}

export const SET_SEX = (state, sex) => {
  state.sex = sex
}

export const SET_ROLE_ID = (state, roleId) => {
  state.roleId = roleId
}

export const SET_ROLES = (state, roles) => {
  state.roles = roles
}

export const SET_MENU = (state, menuList) => {
  state.menuList = menuList
}
